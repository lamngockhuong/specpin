---
title: プライバシーポリシー
description: Specpinのプライバシーとデータ取り扱い方針。ローカルファースト、データ収集なし、トラッキングなし、リモートコードなし。
---

**最終更新日**：2026年6月29日

Specpinはあなたのプライバシーの保護に取り組んでいます。このポリシーでは、ブラウザ拡張機能が収集する（または収集しない）データと、その使用方法を説明します。

## 概要

- ✅ **デフォルトでも、それ以外の場合でも個人データを一切収集しない**
- ✅ **`localhost`上の自分のローカルsidecar以外へのネットワークリクエストなし**
- ✅ **アナリティクス、行動トラッキング、テレメトリ、クラッシュレポートなし**
- ✅ **リモートコードなし：拡張機能はネットワークから何も読み込まず実行しない**
- ✅ **オープンソースで監査可能**

## データ収集

Specpinは個人データを収集、送信、販売、共有しません。クラウドサービス、アナリティクスエンドポイント、テレメトリはありません。拡張機能が行うすべてのことはあなたのマシン上で完結します。

### 収集するもの

何もありません。Specpinは私たちや第三者にデータを送信しません。唯一のネットワーク接続は、**あなた自身が実行する**sidecar（デフォルトでは`localhost`上、オプトインすれば自分のリモートホスト上でHTTPS経由）へのもののみです（[ネットワーク活動](#ネットワーク活動)を参照）。

### ローカルに保存するもの

Specpinはブラウザの拡張機能ストレージを通じて、以下のデータを**あなたのデバイスのみ**に保存します：

| データの種類 | 保存場所 | 目的 | 同期? |
|-----------|------------------|---------|---------|
| 接続設定（sidecar URL + bearerトークン、プロジェクトごと） | `browser.storage` | ローカルsidecarに接続する | いいえ |
| ローカルプロジェクトのspec | `browser.storage` | sidecarなしでspecを作成・表示する | いいえ |
| 設定（テーマ、インターフェース言語、表示モード、デフォルトサーフェス） | `browser.storage` | UIの選択を記憶する | いいえ |

このデータはデバイス上に留まります（接続設定とローカルプロジェクトのspecは、あなたが接続するsidecarにのみ送信されます）。外部ストレージはなく、Specpinのクラウドサービスもありません。

## ブラウザの権限

Specpinは以下の権限を要求します。各権限は説明された目的にのみ使用されます。

### `storage` - データをローカルに保存する

接続設定、ローカルプロジェクトのspec、UIの設定を保存します。すべてのデータはデバイス上に留まります。

### `activeTab` / `tabs` - ページごとにspecをルーティングする

アクティブなタブのoriginを読み取って各ページに正しいプロジェクトのspecをルーティングし、バックグラウンドサービスワーカーからマッチングタブのコンテンツスクリプトにspec更新（ライブリロードイベントを含む）を中継します。

**しないこと**：閲覧履歴をどこかに送信する、アクセスしたページをトラッキングする、閲覧履歴を保存する。

### `alarms` - 接続を維持する

バックグラウンドサービスワーカーがローカルsidecarへのライブリロード（Server-Sent Events）接続を維持するために生き続けられるよう、1分ごとのキープアライブアラームを実行します。ブラウザはアイドル状態のサービスワーカーを終了させます。アラームはそれを起こしてspecストリームの接続を維持します。

### `contextMenus` - 右クリックメニュー

クイックアクション（トグル、キャプチャ、表示モード）のためにページの右クリックメニューに「Specpin」サブメニューを追加します。

### `sidePanel`（Chromeのみ） - サイドパネルサーフェス

ChromeのサイドパネルにSpecpinサーフェスをページの横に開きます。Firefoxは代わりに組み込みの`sidebar_action`を使用します。

### `host_permissions` - `http://127.0.0.1/*`、`http://localhost/*`

バックグラウンドサービスワーカーが`localhost` HTTPとServer-Sent Eventsでローカルsidecarに接続して`.specs/`を読み取り、ライブリロード更新を受信できるようにします。デフォルトでは、Specpinはリモートホストへのリクエストを**一切**行いません。リモートsidecarを追加した場合、拡張機能は接続時にその1つのoriginへのアクセスを要求し（任意かつ取り消し可能な許可）、接続を削除するとそれを取り消します。

## ネットワーク活動

Specpinは**あなた自身が実行する**sidecar（`specpin serve`）にのみ接続します。デフォルトでは`localhost`（`127.0.0.1` / `localhost`）上で、オプトインすれば自分のリモートホスト上（HTTPS経由）で動作します。bearerトークンで認証されたHTTP/HTTPSとServer-Sent Eventsで通信します。

| 接続先 | 使用? |
|-------------|-------|
| ローカルsidecar（`http://127.0.0.1:<port>`、`http://localhost:<port>`） | ✅ 接続を追加した場合のみ |
| 自分のリモートsidecar（`https://<自分のホスト>`） | ✅ リモート接続を追加した場合のみ |
| アナリティクスサービス（Google Analyticsなど） | ❌ 一切なし |
| クラッシュ/エラーレポート（Sentryなど） | ❌ 一切なし |
| 広告ネットワーク | ❌ 一切なし |
| 外部APIやCDN | ❌ 一切なし |
| リモートコード / リモートスクリプト | ❌ 一切なし |

sidecarは強化されています：デフォルトでは`127.0.0.1`にバインドし（リモートバインドには、あなたが運用するHTTPSリバースプロキシが必要です）、bearerトークン認証を要求し、CORS経由でブラウザ拡張機能のoriginのみを受け付け、すべての書き込みを`.specs/`ディレクトリに限定します。sidecarの完全なセキュリティモデルは[セキュリティとプライバシー](/ja/concepts/security-and-privacy/)を参照してください。

## データの共有

Specpinは第三者、広告主、アナリティクスプロバイダー、または他の拡張機能とデータを**共有しません**。共有するデータがありません。何も収集していないためです。

## ブラウザの同期

Specpinはブラウザの同期ストレージを使用しません。接続設定、ローカルspec、設定はそれを設定したデバイスのローカル拡張機能ストレージに保存され、デバイス間で同期されません。

## データの保持と削除

すべてのSpecpinデータは削除するまでブラウザのローカル拡張機能ストレージに存在します。

**拡張機能データをすべて削除するには：**

1. ブラウザの拡張機能ページを開きます（`chrome://extensions`または`about:addons`）。
2. Specpinを見つけて**削除**を選択します。
3. ローカルに保存されているすべての拡張機能データがすぐに削除されます。

specそのものはリポジトリの`.specs/`ディレクトリにあるプレーンなJSONファイルで、Gitを通じてあなたが管理します。

## 子どものプライバシー

Specpinは誰からも（13歳未満の子供や管轄区域の最低年齢に相当する子供を含む）データを収集しません。

## コードの整合性

- **オープンソース**：完全なソースコードは[github.com/lamngockhuong/specpin](https://github.com/lamngockhuong/specpin)
- **監査可能**：誰でもコードをレビューできます
- **リモートコードなし**：すべての拡張機能コードはバンドルされてローカルで実行されます。ネットワークから何もフェッチしません
- **Apache-2.0ライセンス**：自由に検査、変更、再配布できます

## このポリシーの変更

このポリシーは随時更新することがあります。変更は更新された「最終更新日」とともにこのページに掲載され、GitHubリポジトリに反映されます。重要な変更はGitHubリリースノートと拡張機能のストア更新説明に記載します。

## コンプライアンス

Specpinは以下に準拠しています：

- Chrome Web StoreおよびFirefox Add-onの開発者プログラムポリシー
- 一般データ保護規則（GDPR）- データを一切収集しないことによる準拠
- カリフォルニア消費者プライバシー法（CCPA）- データを一切収集しないことによる準拠

Specpinは個人データを収集しないため、私たちの側でアクセス、エクスポート、修正、または削除するデータがありません。すべてのデータはあなたのデバイス上にあり、完全にあなたの管理下にあります。

## お問い合わせ

質問やプライバシーに関する懸念がありましたら：

- **メール**：hi@ohnice.app
- **GitHub Issues**：[github.com/lamngockhuong/specpin/issues](https://github.com/lamngockhuong/specpin/issues)
- **GitHub Discussions**：[github.com/lamngockhuong/specpin/discussions](https://github.com/lamngockhuong/specpin/discussions)

---

**要約**：Specpinはプライバシーファースト、ローカルファーストの拡張機能です。収集も送信もトラッキングも一切しません。唯一のネットワーク接続は、あなた自身が実行するsidecar（デフォルトでは`localhost`上、オプトインすれば自分のリモートホスト上でHTTPS経由）へのものだけです。
